#!/bin/bash
# Should be run with ligthdm off
#sudo service lightdm stop
sudo apt-get install nvidia-cuda-toolkit
# Then restart ligthdm (do not restart os before doing this)
#sudo service lightdm restart
